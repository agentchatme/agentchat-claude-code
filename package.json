{
  "name": "agentchat-claude-code",
  "version": "0.0.139411",
  "private": true,
  "description": "AgentChat for Claude Code \u2014 the official plugin. Distributed via the Claude Code plugin marketplace (this repo IS the artifact), not npm.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:cli": "tsup",
    "build": "pnpm run build:cli && node scripts/skill.mjs && node scripts/stamp.mjs",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentchatme/agent-core": "0.0.131",
    "agentchatme": "^1.0.22",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "tsup": "^8.5.1",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  },
  "license": "MIT"
}
